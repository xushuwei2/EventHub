## 生产环境
### 服务器
ssh match3@52.193.110.105

部署方式：Linux 二进制 + systemd 用户服务

部署目录：`~/eventhub/eventhub/`

本地一键部署：
```powershell
.\script\deploy_prod.ps1
```

手动操作：
```bash
systemctl --user status eventhub
journalctl --user -u eventhub -f
tail -f ~/eventhub/eventhub/log/*.log
```

生产配置：`~/eventhub/eventhub/config/.env`（`HTTP_ADDR` 需为 `:6085`）

### 后端服务器
52.193.110.105:6085
不支持密码登录，需要使用密钥aws-tt.key
防火墙已开启端口:23xxx
对应域名:https://eventhub.bffbond.com

### 数据库
root@HL82yearlf.hotgame
内外网自动切换:database-1.c9ie8y6eckdj.ap-northeast-1.rds.amazonaws.com:33123
