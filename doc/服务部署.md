## 生产环境
### 服务器
ssh match3@52.193.110.105

Docker：**rootless**（`DOCKER_HOST=unix:///run/user/$(id -u)/docker.sock`，登录后 `.bashrc` 已配置）

部署目录：`~/eventhub/eventhub/`

手动操作：
```bash
cd ~/eventhub/eventhub
docker compose -f docker-compose.prod.yml ps    # 版本号见目录下 .env 中 EVENTHUB_VERSION
docker compose -f docker-compose.prod.yml logs -f eventhub
```

### 后端服务器
52.193.110.105:6085
不支持密码登录，需要使用密钥aws-tt.key
防火墙已开启端口:23xxx
对应域名:https://eventhub.bffbond.com

### Docker 镜像
- 仓库：`jpccr.ccs.tencentyun.com/eventhub/eventhub:{tag}`

### 数据库
root@HL82yearlf.hotgame
内外网自动切换:database-1.c9ie8y6eckdj.ap-northeast-1.rds.amazonaws.com:33123